

server:
  port: 9091
spring:
  application:
    name: sc-order
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/springcloudsell?useUnicode=true&serverTimezone=GMT%2B8&useSSL=false&characterEncoding=utf8
  jpa:
    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost-order